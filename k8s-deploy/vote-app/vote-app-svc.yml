apiVersion: v1
kind: Service
metadata:
    name: vote-app-svc
    labels:
      app: vote-app-svc
      type: frontend
spec:
    type: NodePort
    selector: 
      app: vote-app
      type: frontend
    ports:
     - port: 80
       targetPort: 80
       nodePort: 30015